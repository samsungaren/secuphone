<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Cache directory paths -->
    <cache-path
        name="cache"
        path="." />
    <external-cache-path
        name="external_cache"
        path="." />
    <external-cache-path
        name="temp_files"
        path="temp_view/" />
    <external-cache-path
        name="shared_files"
        path="shared_files/" />
        
    <!-- External storage paths -->
    <external-path
        name="external"
        path="." />
    <external-path
        name="external_files"
        path="Download/" />
        
    <!-- App-specific storage -->
    <external-files-path
        name="external_files_path"
        path="." />
    <files-path
        name="files"
        path="." />

    <!-- Specific paths for encrypted files functionality -->
    <cache-path 
        name="temp_decrypted" 
        path="temp_decrypted/" />
    
    <!-- Ensure fallback access to all cache subdirectories -->
    <cache-path 
        name="all_cache_dirs" 
        path="." />
</paths> 